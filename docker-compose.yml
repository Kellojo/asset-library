services:
  asset-library:
    image: ghcr.io/kellojo/asset-library:latest
    environment:
      - ORIGIN=http://localhost:3000
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
