version: "3.8"
services:
  asset-library:
    build:
      context: .
      dockerfile: Dockerfile
    image: asset-library:latest
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
